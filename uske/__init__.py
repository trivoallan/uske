"""The orchestrator seed: the decisions of one screening, exposed as subcommands.

Argo Workflows chains the steps; `regis` evaluates and `knock` places, each in its own image.
Spec: spec.md §5.2; change `socle-amorce-orchestrateur`.
"""
