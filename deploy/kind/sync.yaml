# One dt-sync pass, started by hand (the CronWorkflow is suspended in kind).
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: dt-sync-
spec:
  workflowTemplateRef:
    name: dt-sync
